apiVersion: v1
kind: Route
metadata:
  name: "wordpress"
  labels:
    app: "wordpress"
spec:
  host: 'wordpress.apps-crc.testing'
  to:
    kind: Service
    name: "wordpress"
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
